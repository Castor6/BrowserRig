---
"browserrig": minor
---

Add opt-in native WebMCP discovery and tool invocation for session pages through the existing browser extension, with automatic tool definitions in CLI, MCP, and DSH results.
