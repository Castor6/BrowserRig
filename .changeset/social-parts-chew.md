---
"browserrig": patch
---

Recover operational commands from safely identifiable stale managed relays, wait through brief extension reconnects in the TypeScript client, and report sessions connected only when their default page is live.
